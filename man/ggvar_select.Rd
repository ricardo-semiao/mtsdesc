% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggvar_select.R
\name{ggvar_select}
\alias{ggvar_select}
\title{Plot VAR information criteria for lag selection}
\usage{
ggvar_select(
  x,
  series = NULL,
  lag.max = 10,
  type = "const",
  criteria = c("AIC", "HQ", "SC", "FPE"),
  trans = "none",
  args_aes = list(),
  args_line = list(),
  args_labs = list(),
  ...
)
}
\arguments{
\item{x}{A dataset (object coercible to data.frame) to pass to
\link[vars:VARselect]{VARselect}, or, directly, a result of such call.}

\item{series}{A character vector with series (variables) to  consider. Defaults to all (\code{NULL}).}

\item{lag.max}{Integer for the highest lag order. Passed to
\link[vars:VARselect]{VARselect}. Unused if \code{x} is of class list.}

\item{type}{Type of deterministic regressors to include. Passed to
\link[vars:VARselect]{VARselect}. Unused if \code{x} is of class list.}

\item{criteria}{The criteria to be considered. Any of "AIC", "HQ", "SC",
and "FPE".}

\item{trans}{A transformation to apply to each criteria result (vector). Can
be a function, "none" (the default), or "index" to create index numbers.}

\item{args_line}{Additional arguments passed to \link[ggplot2:geom_path]{geom_line} (respectively). See more in the 'Customization' section.}

\item{args_labs}{Additional arguments passed to \link[ggplot2:labs]{labs}. If an empty list, will be changed to default values.}

\item{...}{Arguments passed to methods, see the 'Methods' section.}
}
\value{
A \link[ggplot2:ggplot]{ggplot}.
}
\description{
Plots the result of a call to \link[vars:VARselect]{VARselect}.
}
\details{
\subsection{Customization}{

The graph can be customized both with the 'static' arguments passed to each layer -- using the \verb{args_*} arguments --, and, if applicable, the 'dynamic' aesthetics -- using the \code{args_aes} argument.

The \code{args_aes} is a list with '* = arguments to \verb{scale_*_manual} \
elements, where '*' represents the name of an aesthetic to apply to the \
data. View \link{vignette('ggplot2-specs', 'ggplot2')} to see the available \
aesthetics.

After built, the result can be further customized as any ggplot, adding or overwriting layers with the \link[ggplot2:gg-add]{ggplot's +}. It is useful to understand the data and the mappings coded by the package, using the function \link{find_gg_info}.

See \link{vignette('customizing-graphs')} for more details.
}

\subsection{Methods}{

The data from \code{x} is extracted with the generic function {texts_vec}. Each class conditions an external function to pass the \code{...} arguments to. Below there is a list with all the currently implemented classes:
\itemize{
\item Class \code{'list'}: passed to nothing.
\item Class \code{'default'}: passed to \link[vars:VARselect]{vars::VARselect}.
}
}
}
\examples{
ggvar_select(vars::VARselect(freeny[-2]))

}
\seealso{
Other model diagnostics plots: 
\code{\link{ggvar_acf}()},
\code{\link{ggvar_dispersion}()},
\code{\link{ggvar_distribution}()},
\code{\link{ggvar_history}()},
\code{\link{ggvar_stability}()}
}
\concept{model diagnostics plots}
